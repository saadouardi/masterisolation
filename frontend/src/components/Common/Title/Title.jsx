export const Title = ({title})=>{
    return(
        <>
            <div className="section__title_div">
                <h1 className="section__title">{title}</h1>
            </div>
        </>
    )
}